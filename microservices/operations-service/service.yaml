apiVersion: v1
kind: Service
metadata:
  name: operations-service
  namespace: operations-service
  labels:
    app: operations-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: operations-service
