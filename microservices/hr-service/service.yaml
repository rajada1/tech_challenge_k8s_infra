apiVersion: v1
kind: Service
metadata:
  name: hr-service
  namespace: hr-service
  labels:
    app: hr-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: hr-service
