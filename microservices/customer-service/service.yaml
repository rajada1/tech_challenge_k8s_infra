apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: customer-service
  labels:
    app: customer-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: customer-service
